name: 自动生成群晖地址的插件文件

on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  PYTHONIOENCODING: utf-8

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 配置Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 安装依赖
        run: |
          pip install --upgrade pip
          pip install loguru httpx ujson
          pip list | grep -E "loguru|httpx|ujson"

      - name: 生成插件文件（核心：保留原始文件名）
        run: |
          # 提前创建日志文件
          touch build.log
          # 确保dist目录存在
          mkdir -p ./dist
          # 运行脚本并记录日志
          python src/main.py 2>&1 | tee -a build.log
          # 验证文件生成
          if [ -f "./dist/plugins.json" ]; then
            echo "✅ 插件文件生成成功！预览URL（保留原始文件名）："
            grep -E '"url": "http://7se.de5.net:8888/music/"' ./dist/plugins.json | head -5
            # 复制到临时目录（避免分支切换丢失）
            mkdir -p ./temp_dist
            cp -r ./dist/* ./temp_dist/
            # 验证临时目录文件
            echo "✅ 临时目录文件列表："
            ls -l ./temp_dist/
          else
            echo "❌ 插件文件生成失败"
            cat build.log
            exit 1
          fi

      - name: 推送文件到dist分支
        run: |
          # 配置git用户
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          # 切换到dist分支（无则创建）
          git checkout --orphan dist
          # 清空旧内容
          rm -rf *
          # 从临时目录复制文件（路径绝对正确）
          cp -r ./temp_dist/* .
          # 验证复制结果
          echo "✅ dist分支文件列表："
          ls -l
          # 提交并推送
          git add .
          git commit -m "Auto update plugins (保留原始文件名) $(date +'%Y-%m-%d %H:%M:%S')"
          git push -f origin dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 上传日志
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: build.log
        if: always()
